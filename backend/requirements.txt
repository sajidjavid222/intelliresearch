fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.6
httpx>=0.28
python-dotenv>=1.0
SQLAlchemy>=2.0.36
aiosqlite>=0.20
asyncpg>=0.29        # Postgres driver (production); SQLite used locally
greenlet>=3.1
feedparser>=6.0
python-multipart>=0.0.17
python-jose[cryptography]>=3.3
bcrypt>=4.0
email-validator>=2.1
# Caching: the app uses Redis when REDIS_URL is reachable, else an in-memory
# fallback. Installing the client does NOT require a running Redis server.
redis>=5.2

# Proposal export to DOCX / PDF.
python-docx>=1.1
reportlab>=4.2

# Chat-with-PDF: extract text from uploaded PDFs (pure-Python, no system deps).
pypdf>=4.0

# Optional extras (uncomment to enable):
# anthropic>=0.40
# openai>=1.57
# celery>=5.4
# qdrant-client>=1.12
